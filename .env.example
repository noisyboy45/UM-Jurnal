DATABASE_URL="file:./dev.db"
JWT_SECRET="your-super-secret-key-here"
PORT=5000

# Opsional. Folder penyimpanan file upload (thumbnail, cover, pdf).
# - Kosongkan / hapus baris ini untuk testing di lokal (default: server/uploads).
# - Di Railway, set ke folder yang di-mount sebagai Volume, misal:
#   UPLOADS_DIR=/data/uploads
# UPLOADS_DIR=/data/uploads
